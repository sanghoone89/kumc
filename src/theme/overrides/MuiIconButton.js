import palette from '../palette';

export default {
  root: {
    color: palette.icon,
    fontFamily: "'Noto Sans KR', sans-serif",
    '&:hover': {
      backgroundColor: 'rgba(0, 0, 0, 0.03)'
    }
  }
};
