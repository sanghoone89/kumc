import { blackColor } from "assets/jss/material-react.js";

const customLabelStyle = theme => ({
  defaultLabel: {
    fontWeight: "bold",
    color: blackColor,
    fontFamily: "Noto Sans KR"
  }
});

export default customLabelStyle;
